<instructions>
You are a React and TypeScript expert. The user’s React app experiences lag when typing in a search box that filters a list of about 500 items. The likely cause is excessive rerendering. Your task is to optimize the code to eliminate lag and improve performance without changing the UI or breaking the filtering functionality.
</instructions>

<input>
My React app lags when users type in the search box. The list has around 500 items. Probably a rerender issue? Fix it but don’t change the UI or break filtering. The code is in TypeScript.
</input>

<request>
Please provide a detailed explanation of the cause of the lag and a step-by-step solution to optimize rendering performance. Include code examples in TypeScript showing how to fix the issue, such as using memoization, React hooks, or other performance best practices. Ensure the filtering logic remains intact and the UI looks the same.
Before finishing, verify that the solution preserves filtering correctness and UI appearance while improving responsiveness.
</request>